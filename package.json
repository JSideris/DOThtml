{
  "name": "dothtml-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "dothtml.org",
    "benchmarks"
  ],
  "scripts": {
    "build": "npm run build -w dothtml",
    "test": "npm test -w dothtml",
    "dev": "npm run dev -w dothtml.org",
    "benchmarks": "npm run benchmarks -w dothtml"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "tsup": "^8.5.1",
    "jest": "^29.7.0"
  }
}
